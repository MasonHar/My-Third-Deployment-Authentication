# Screenshots

Add your assignment screenshots here:

- `postman/` — Postman requests (register, login, me, change-password, transactions)
- `browser/` — Browser visits to `GET /` and any other URLs you test

Browser testing of JSON APIs is limited to viewing raw JSON in the address bar (no built-in UI). Postman is the primary tool for authenticated POST/PATCH requests.
